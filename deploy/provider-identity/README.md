# Provider identity
This directory contains a fake identity for testing purposes. The JWT wes genereted using the private key and can be verified using the vc-verifier.

If this information changes, mockserver-config.yaml have to be updated accordingly.